{
  "name": "vue-wc-model",
  "version": "0.0.1",
  "description": "Adds a compiler directive in order to allow for v-model ('v-wcmodel') on custom components.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "Thomas Caserta <tom@caserta.co.uk>",
  "license": "MIT",
  "url": "https://github.com/TomCaserta/vue-wc-model",
  "devDependencies": {
    "typescript": "^3.1.3"
  }
}
